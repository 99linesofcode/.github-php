name: test

on:
  push:
    branches-ignore:
      - main
  pull_request:
    branches-ignore:
      - main

permissions:
  contents: read

jobs:
  test:
    uses: 99linesofcode/.github/.github/workflows/test.yaml@main
    with:
      calling_repository_name: ${{ github.event.repository.name }}
